const car = {
    price: 2000,
    color: "red",
    numDoors: 4
};

if (car.price < 2000 || car.color == "red" || car.numDoors == 4){
    console.log("We are going to take it");
} else {
    console.log("We are keep looking");
}